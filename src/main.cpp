#include "expression.h"
//#include <fraction.h>
#include <iostream>

int main()
{
    //    Fraction fr(std::numeric_limits<int64_t>::min());
    //    Fraction f(0);
    //    std::cout << (fr > f) << std::endl;
    //    Variable c("x");
    const Fraction m(-1, -1);

    std::cout << m;
    //    std::cout << c.eval({{"x", 6}}) << std::endl;
    //    Add sum(Const(1), Variable("x"));
    //    auto sum2 = Const(1) + Const(1) - Const(1) / Const(1) * Const(1);
    //    std::cout << "yes" << std::endl;
    //    std::cout << sum.to_string() << std::endl;
    //    for (int i = 0; i < 10; ++i) {
    //        std::cout << "1 + " << i << " = " << sum.eval({{"x", i}}) << std::endl;
}
